리눅스에서 많이 보는 압축 파일의 확장명
xz, bz2, gz, zip Z등 이 있음
예전에는 gz를 사용
최근에는 압축률이 더 좋은 xz 혹은 bz2를 많이 사용

xz
확장면 xz로 압축하거나 풀어줌 - 비교적 최신 압축 명령 - 압축률이 뛰어남

xz 파일이름 > '파일 이름'을 압출파일인 파일이름.xz로 만듬, 기존 파일 삭제
xz -d 파일이름.xz > d= Decompress
                   파일이름.xz 압축파일을 일반 파일인 '파일이름'으로 만듬
xz -l 파일이름.xz > l = List
                   파일이름.xz 압축 파일에 포함된 파일 목록과 압출륙 등을 출력
xz -k 파일이름    > k = keep
                   압축 후에 기존 파일을 삭제하지 않고 그대로 둠
                   
                   
bzip2
확장명 bz2로 압축하거나 풀어줌

bzip2 파일이름  > 파일이름 을 압축 파일인 파일이름.bz2로 만듬
bzip2 -d 파일이름.bz2 > 파일이름.bz2 압축 파일을 일반 파일인 파일이름 으로 만듬

bunzip2
확장명 bz2의 압축을 풀어줌
bzip2 -d 와 동일한 명령어

gzip
확장명 gz로 압축하거나 풀어줌

gzip 파일이름 > 파일이름 을 압축 파일인 파일명.gz로 만듬
gzip -d 파일이름.gz > 파일이름.gz 압축 파일을 일반 파일인 파일이름 으로 만듬


gunzip
확장명 gz의 압축을 풀어준다
gzip -d 와 동일한 명령어


zip
Windows용과 호환되는 확장명 zip으로 압축하거나 풀어준다

zip 생성할파일이름.zip 압축할파일이름 > 압축할 파일 이름을 '새로 생성될 파일이름.zip'으로 만듬


unzip
windows 용과 호환되는 zip으로 묶은 압축 파일을 풀어준다
unzip 압축파일이름.zip  > 압축파일이름.zip 의 압축을 
